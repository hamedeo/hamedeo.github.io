---
import { getEntry, render } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import LifeJourney from "../components/life/LifeJourney.astro";
import AboutCurrent from "../content/about-current.md";
import { PAGES } from "../config";
import "../styles/pages/about.css";

// Load bio content
const bioEntry = await getEntry("bio", "bio");
const { Content: BioContent } = bioEntry
	? await render(bioEntry)
	: { Content: null };
---

<BaseLayout title={PAGES.home.title}>
	<div class="container-narrow">
		{
			(PAGES.home.title || PAGES.home.subtitle) && (
				<header class="page-header">
					{PAGES.home.title && (
						<h1 class="page-title">{PAGES.home.title}</h1>
					)}
					{PAGES.home.subtitle && (
						<p class="page-subtitle">{PAGES.home.subtitle}</p>
					)}
				</header>
			)
		}
		<div class="prose about-introduction">
			{BioContent ? <BioContent /> : <p>Loading bio...</p>}
		</div>
		<LifeJourney />
		<div class="prose about-current">
			<AboutCurrent />
		</div>
	</div>
</BaseLayout>
